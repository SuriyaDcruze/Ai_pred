# Serving dependencies for Render (CPU-only torch keeps the image small).
# Training-only deps (tensorboard, torchmetrics) are intentionally excluded.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.3
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
pydantic>=2.7
pydantic-settings>=2.3
fastapi>=0.111
uvicorn[standard]>=0.30
websockets>=12.0
httpx>=0.27
python-dotenv>=1.0
rich>=13.7
